Workspace Management API -
A role-based workspace management backend system built with Node.js, Express, TypeScript, Prisma, and PostgreSQL.
It supports authentication, workspace management, projects, tasks, invitations, comments, notifications, and activity tracking.

Tech Stack -
Node.js, Express, TypeScript, Prisma, PostgreSQL, JWT, bcrypt, Zod, Multer

I did today by fixing task routes and controller , by adding status query in tasks get request controller and project routes and controller too by adding search and status query

Today I fixed the task routes and controllers, added status query filtering to the task GET request controller, fixed the project routes and controllers, added search and status query filtering to the project GET request, and implemented the member dashboard with assigned projects, assigned tasks, task statistics, and pagination.
